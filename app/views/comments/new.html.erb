<h2 class="text-center new-comment-top">新增评论</h2>

<div class="col-md-4 col-md-offset-4">
  <%= simple_form_for [@board, @post, @comment] do |f| %>
    <div class="group">
      <%= f.input :content %>
    </div>
    <div class="group">
      <%= f.button :submit, "提交"  %>
    </div>
    <% end %>
</div>
