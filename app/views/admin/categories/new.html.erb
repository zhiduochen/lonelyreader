<h1>New Category</h1>

<%= simple_form_for [:admin, @category] do |f| %>
  <div class="group">
    <%= f.input :name %>
  </div>
  <div class="group">
    <%= f.submit "提交", data: {disable_with: "提交中..."}%>
  </div>
<% end %>
